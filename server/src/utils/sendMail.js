// Placeholder email sender
export const sendMail = async ({ to, subject, text, html, attachments }) => {
  // In Phase 3, implement with nodemailer
  return { success: true, message: 'Email queued (mock)' }
}



