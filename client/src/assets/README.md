# Assets Directory

This directory contains static assets like images, fonts, and other media files.

## Future Assets (Phase 2)

- `logo.svg` - ChemAI Lab logo
- `icons/` - Icon set for UI elements
- `images/` - Images for experiments and UI
- `fonts/` - Custom fonts if needed


